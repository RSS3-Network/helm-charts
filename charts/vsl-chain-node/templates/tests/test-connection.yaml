apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vsl-chain-node.fullname" . }}-test-connection"
  labels:
    {{- include "vsl-chain-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vsl-chain-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
