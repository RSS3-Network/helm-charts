apiVersion: v1
kind: Service
metadata:
  name: {{ include "vsl-sequencer.fullname" . }}
  labels:
    {{- include "vsl-sequencer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vsl-sequencer.selectorLabels" . | nindent 4 }}
