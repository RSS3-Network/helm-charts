apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vsl-sequencer.fullname" . }}-test-connection"
  labels:
    {{- include "vsl-sequencer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vsl-sequencer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
